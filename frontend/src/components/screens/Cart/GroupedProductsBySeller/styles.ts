import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  storeName: {
    marginLeft: 10,
    fontSize: 16,
    fontWeight: "500",
    paddingBottom: 5,
    paddingTop: 10,
  },
  container: {
    shadowColor: "rgba(0, 0, 0, 0.7)",
    elevation: 35,
    borderRadius: 15,
    width: "100%",
    backgroundColor: "#fff",
    marginTop: 8,
    marginBottom: 8,
  },
});
