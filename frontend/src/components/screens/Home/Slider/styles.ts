import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    alignItems: "center",
    backgroundColor: "#fff",
    width: "100%",
    paddingTop: 15,
  },
  image: {
    width: "85%",
    height: 180,
    borderRadius: 15,
  },
});
