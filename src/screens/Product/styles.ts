import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  scrollView: {
    height: "100%",
  },
  container: {
    display: "flex",
    alignItems: "center",
    position: "relative",
    zIndex: 0,
    backgroundColor: "#fff",
  },
  logo: {
    width: "100%",
    height: "65%",
  },
});
