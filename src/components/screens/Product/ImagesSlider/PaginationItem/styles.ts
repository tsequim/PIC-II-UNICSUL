import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "white",
    width: 10,
    height: 10,
    marginHorizontal: 10,
    borderRadius: 100,
    overflow: "hidden",
    transform: [
      {
        rotateZ: "0deg",
      },
    ],
  },
  animatedView: {},
});
